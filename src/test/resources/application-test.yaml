oracle:
  scheduling:
    enabled: false
  private-key-path: classpath:key/private.pem

spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
  liquibase:
    url: jdbc:h2:mem:shieldx
    password:
    enabled: true
    user: sa
    change-log: classpath:db/changelog/db.changelog-master.yaml