spring:
  application:
    name: oracle
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
klever:
  api:
    base-url: https://api.mainnet.klever.org
    timeout: 3000ms